import React from "react";

export default function About(){
    return(
        <div>Hola, soy Meli!</div>
    )
}

/* Cuando construimos un componente:
 1. importar react
 2. si es funcional: return -> devuelve directamente lo que queremos renderizar
                    props? recibe por parametros
*/
